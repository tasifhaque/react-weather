import { useWeather } from "@/store/weather.store";
import { useGeoLocation } from "@/store/geolocation.store";
import { useTheme } from "@/store/theme.store";

export { useWeather, useGeoLocation, useTheme };
